<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.djordje.seeds.SettingsActivity">

    <LinearLayout
        android:id="@+id/settings_main_linearlayout"
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:layout_height="match_parent">
        <android.support.v7.widget.Toolbar
            android:id="@+id/settings_toolbar"
            app:popupTheme="@style/AppTheme.PopupOverlay"
            app:title="SETTINGS"
            app:titleTextColor="@color/mdtp_white"
            app:logo="@drawable/ic_seeds_icon"
            android:fitsSystemWindows="true"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:logoDescription="SETTINGS"
            app:titleMarginStart="32dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            android:minHeight="?attr/actionBarSize"/>
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="@dimen/activity_vertical_margin"
            android:paddingLeft="@dimen/activity_horizontal_margin"
            android:paddingRight="@dimen/activity_horizontal_margin"
            android:paddingTop="@dimen/activity_vertical_margin">
            <LinearLayout
                android:layout_width="match_parent"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:orientation="horizontal"
                android:layout_height="wrap_content">
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textStyle="bold"
                    android:textColor="@color/textColorPrimary"
                    android:textSize="18sp"
                    android:text="Polling interval (minutes): "/>
                <EditText
                    android:layout_width="80sp"
                    android:id="@+id/polling_interval"
                    android:textAlignment="center"
                    android:layout_height="wrap_content"/>
            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:orientation="horizontal"
                android:id="@+id/check_interval_linearlayout"
                android:layout_height="wrap_content">
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textStyle="bold"
                    android:textColor="@color/textColorPrimary"
                    android:textSize="18sp"
                    android:text="Check Interval (minutes): "/>
                <EditText
                    android:layout_width="80sp"
                    android:id="@+id/check_interval"
                    android:gravity="center"
                    android:textAlignment="center"
                    android:layout_height="wrap_content"/>
            </LinearLayout>
        </LinearLayout>
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:backgroundTint="@color/colorPrimary"
            android:background="@android:color/darker_gray"/>

        <ListView
            android:id="@+id/device_schedule_list"
            android:layout_width="match_parent"
            android:divider="@color/colorPrimary"
            android:dividerHeight="1dip"
            android:descendantFocusability="afterDescendants"
            android:layout_height="wrap_content">
        </ListView>

    </LinearLayout>

    <android.support.design.widget.FloatingActionButton
        android:layout_width="wrap_content"
        android:id="@+id/settings_done_button"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:layout_anchorGravity="bottom|right|end"
        app:layout_anchor="@id/settings_toolbar"
        />

</android.support.design.widget.CoordinatorLayout>
